var documenterSearchIndex = {"docs":
[{"category":"section","location":"api/#API-reference","page":"API reference","text":"Public interface of HMatrixGPU.jl. Docstrings of the shipped code are rendered below; every entry also appears in context on the manual page.","title":"API reference"},{"category":"section","location":"api/#Trees","page":"API reference","text":"","title":"Trees"},{"category":"section","location":"api/#Kernel-matrix-(lazy,-matrix-free)","page":"API reference","text":"KernelMatrix is the lazy kernel behind the high-level HMatrix constructors and can also be held and passed around on its own: every utility that accepts an AbstractMatrix (e.g. sparsify_hmatrix) composes with it directly (see the manual).","title":"Kernel matrix (lazy, matrix-free)"},{"category":"section","location":"api/#Hierarchical-matrix","page":"API reference","text":"In addition to the documented low-level constructor above, HMatrix has four high-level methods — thin wrappers that build the cluster trees, wrap g in a KernelMatrix and forward to the low-level constructor (no parallel implementation):\n\nForm Signature Notes\nkernel + point set (square; do-block friendly) HMatrix(g::Function, pts; dims = 1, max_points_per_leaf = 32, kwargs...) targets = sources = pts\nkernel + point sets (rectangular) HMatrix(g::Function, pts_t, pts_s; dims = 1, max_points_per_leaf = 32, kwargs...) device point sets are legal (downloaded once); the placement is backend= (default CPU())\nkernel + custom trees HMatrix(g::Function, X::ClusterTree, Y::ClusterTree; dims = nothing, kwargs...) dims defaults to the trees' dims; a mismatch errors\nown lazy K + point sets HMatrix(K::AbstractMatrix, pts_t, pts_s; dims = 1, max_points_per_leaf = 32, kwargs...) the library builds the trees; lands on backend= (default CPU())\n\nkwargs... are the low-level keywords (eta, eps, index_map_using_cpu, svd_recompress, row_block_size, col_block_size, backend). HMatrix(pts; ...) do x, y ... end is the do-block spelling of the first form. The contracts (kernel sentence, dims layout, backend resolution) are described on the manual page.","title":"Hierarchical matrix"},{"category":"section","location":"api/#Products","page":"API reference","text":"","title":"Products"},{"category":"section","location":"api/#Diagnostics","page":"API reference","text":"","title":"Diagnostics"},{"category":"section","location":"api/#Plotting-(optional-Plots.jl-extension)","page":"API reference","text":"plot_hmatrix(H::HMatrix; nx = 600, ny = 600, kwargs...) -> Plots.Plot — heatmap of the block distribution: teal = dense (near-field) blocks, amber = low-rank (far-field) blocks, origin (0, 0) at the bottom left (matrix entry (1, 1) sits there). No title or axis labels, so a caption can be provided externally; nx/ny set the raster resolution and extra kwargs... go to Plots.heatmap. Save the result with Plots.savefig(p, \"hmatrix_pattern.png\").\n\nThe method is provided by the HMatrixGPUPlotsExt package extension and becomes available once Plots.jl is loaded in the session (using Plots). Both functions are exported.","title":"Plotting (optional Plots.jl extension)"},{"category":"section","location":"api/#Backends-and-utilities","page":"API reference","text":"The remaining backend/allocation helpers are unexported shims over KernelAbstractions (documented here by signature):\n\ncreate_zeros(B, T, dims...) / create_ones(B, T, dims...) — backend-generic allocation, i.e. KernelAbstractions.zeros(B, T, dims...) and ones.\nkernel_array(B, a) — move the array a to the backend B (an alias of move_to_backend).\nHMatrixGPU.set_groupsize(n) — set the workgroup size (default 512) used to launch the matvec's permutation kernel; a global performance knob that never changes results (see the manual).\n@using_gpu() — try-load the four vendor packages (CUDA, AMDGPU, oneAPI, Metal); a plain convenience loader with no selection logic.\n\nThe backend resolution rules (the explicit backend= keyword, default CPU(), strict name resolution, no global state) are described on the manual page; backend_from_name is the strict name resolver the backend= keyword uses.","title":"Backends and utilities"},{"category":"section","location":"api/#Low-rank-approximation-(internal,-documented-for-reference)","page":"API reference","text":"ACA_plus is the adaptive cross approximation used for every low-rank block on the CPU assembly path. It is an internal algorithm — documented here for reference and because the test suite relies on it.","title":"Low-rank approximation (internal, documented for reference)"},{"category":"type","location":"api/#HMatrixGPU.ClusterTree","page":"API reference","text":"ClusterTree(coordinates; max_points_per_leaf::Int=32, dims::Int=1) -> ClusterTree\n\nBuilds a hierarchical cluster tree from a set of points.\n\ncoordinates accepts two forms (normalized once, no numeric preprocessing — coordinate values are used as given):\n\na d x N matrix with one point per column (stored by reference), or\na vector of points, each a d-element tuple or vector (stacked once into a d x N matrix).\n\nDevice-resident coordinates are downloaded to the host once — tree construction is a host-side geometric algorithm. An input with many more rows than columns warns about a possible transposed (N x d) layout.\n\nArguments\n\ncoordinates: the point set, in either of the two forms above.\nmax_points_per_leaf::Int=32: The maximum number of points per leaf node. If a node has more points than this threshold,  it will be split along its longest axis to form child nodes. Defaults to 32.\ndims::Int=1: The dimensionality factor applied to the point indices in index_map. When greater than 1, index_map will be expanded to account for the specified dimension.\n\nReturns\n\nA ClusterTree struct containing:\n\nindex_map: A reordered vector of point indices that reflects the cluster structure.\nroot_node: The root ClusterNode representing the entire hierarchical cluster tree.\ndims: The DOFs-per-point factor (the default ACA block sizes follow it).\ncoordinates: The normalized d x N host coordinates (one point per column).\n\nExample\n\ncoordinates = rand(3, 100)  # 100 points in 3D space\ntree = ClusterTree(coordinates; max_points_per_leaf=5, dims=3)\n\n\n\n\n\n","title":"HMatrixGPU.ClusterTree"},{"category":"type","location":"api/#HMatrixGPU.KernelMatrix","page":"API reference","text":"KernelMatrix(g, targets, sources; dims::Int = 1)\n\nLazy, matrix-free kernel matrix: the entry contract is K[i,j] = g(x_i, y_j) — g receives the coordinates (column views) of the i-th target and j-th source point, not indices. For dims = d > 1 (vector kernels, g returning a d x d matrix) the DOFs are flattened as K[d(p-1)+c, d(q-1)+e] = g(x_p, y_q)[c, e]. Point sets accept both forms (d x N matrix / vector of points); coordinates are used exactly as given (no scaling). g must be total: regularize coincident points inside g (see the examples).\n\n\n\n\n\n","title":"HMatrixGPU.KernelMatrix"},{"category":"type","location":"api/#HMatrixGPU.HMatrix","page":"API reference","text":"mutable struct HMatrix\n\nHierarchical matrix stored as three CSR-compressed operators on the active backend (CPU, CUDA, AMDGPU, oneAPI or Metal via KernelAbstractions). The matrix-vector product runs as three thread-group kernels without shared memory, barriers or atomics:\n\nxord = x[source_index_map] — the input vector permuted into cluster order,\nvx = V * xord                — one workgroup per rank row of the far field,\ny[tmap[i]] = (D + U) * [xord; vx] — one workgroup per matrix row.\n\nSince the near-field and U row sets partition the matrix rows and the target index map is a permutation, every entry of y is written exactly once.\n\nFields\n\nm::Int, n::Int: dimensions of the full matrix.\ntarget_index_map::AbstractVector{Int}: permutation from cluster-ordered rows to original row indices.\nsource_index_map::AbstractVector{Int}: permutation from cluster-ordered columns to original column indices.\nnear_rowptr/near_colval/near_data: CSR of the near-field (dense) blocks. Column indices are cluster positions (Int32).\nv_rowptr/v_colval/v_data: CSR of the far-field V factors, one row per rank row of every admissible block.\nu_rowptr/u_colval/u_data: CSR of the far-field U factors, rows in cluster order; column indices address the vx buffer.\nvx_buffer::AbstractVector{T}: intermediate buffer (length = total rank rows).\nx_buffer::AbstractVector{T}: input vector permuted into cluster order.\nranks::Vector{Int}: rank of each admissible block (host side, for info).\nndense::Int, napprox::Int: number of near-field and far-field blocks.\n\n\n\n\n\n","title":"HMatrixGPU.HMatrix"},{"category":"function","location":"api/#LinearAlgebra.mul!","page":"API reference","text":"mul!(result::AbstractArray{T}, hmatrix::HMatrix{T}, x::AbstractArray{T}) where T\n\nPerforms a matrix-vector multiplication using the CSR-compressed hierarchical matrix (HMatrix), storing the result in the provided array result. result is fully overwritten.\n\nArguments\n\nresult::AbstractArray{T}: Preallocated array to store the result of the matrix-vector multiplication (same backend as x and the HMatrix).\nhmatrix::HMatrix{T}: The hierarchical matrix used for the multiplication.\nx::AbstractArray{T}: The input vector to be multiplied.\n\n\n\n\n\n","title":"LinearAlgebra.mul!"},{"category":"function","location":"api/#Base.:*","page":"API reference","text":"*(hmatrix::HMatrix{T}, x::AbstractArray{T}) where T\n\nOverloaded multiplication operator for HMatrix. Performs the matrix-vector multiplication and returns a newly allocated result array on the same backend as x.\n\nArguments\n\nhmatrix::HMatrix{T}: The hierarchical matrix used for the multiplication.\nx::AbstractArray{T}: The input vector to be multiplied.\n\nReturns\n\nresult::AbstractArray{T}: The result of the matrix-vector multiplication.\n\n\n\n\n\n","title":"Base.:*"},{"category":"function","location":"api/#HMatrixGPU.info","page":"API reference","text":"info(tree::ClusterTree) -> Dict{Symbol, Any}\n\nAnalyzes the given ClusterTree to extract basic statistics.\n\nArguments\n\ntree::ClusterTree: The hierarchical clustering tree to be analyzed.\n\nReturns\n\nA dictionary containing:\n\ndepth: Total number of depths in the tree.\nmax_points: Maximum number of points in any leaf node.\nmin_points: Minimum number of points in any leaf node.\n\n\n\n\n\ninfo(hmatrix::HMatrix) -> Dict\n\nReturns information about the CSR-compressed HMatrix: size, block counts, rank range and the compression ratio.\n\n\n\n\n\n","title":"HMatrixGPU.info"},{"category":"function","location":"api/#HMatrixGPU.sparsify_hmatrix","page":"API reference","text":"sparsify_hmatrix(K, X, Y; eta=1.5)\n\nConvert dense blocks of an H-matrix to a sparse matrix representation for efficient storage and computation.\n\nArguments\n\nK::AbstractMatrix: Original dense matrix to be sparsified.\nX::ClusterTree: Cluster tree defining row partitioning.\nY::ClusterTree: Cluster tree defining column partitioning.\neta::Float64=1.5: Admissibility parameter for block clustering (controls block tree structure).\n\nReturns\n\nSparseMatrixCSC: Sparse matrix containing all dense blocks from the H-matrix structure.\n\n\n\n\n\n","title":"HMatrixGPU.sparsify_hmatrix"},{"category":"function","location":"api/#HMatrixGPU.hmatrix_blocks","page":"API reference","text":"hmatrix_blocks(H::HMatrix) -> (dense, lowrank)\n\nReconstruct the leaf blocks of the assembled HMatrix from its CSR index arrays (works for CPU and GPU-resident matrices; only the index arrays are downloaded). Returns (dense, lowrank) where dense is a vector of (rows, cols) index ranges and lowrank a vector of (rows, cols, rank).\n\n\n\n\n\n","title":"HMatrixGPU.hmatrix_blocks"},{"category":"function","location":"api/#HMatrixGPU.to_backend","page":"API reference","text":"to_backend(like, a)\n\nMove the array a to the backend where like lives (\"backend follows the data\"); a no-op when both live on the same backend.\n\n\n\n\n\n","title":"HMatrixGPU.to_backend"},{"category":"function","location":"api/#HMatrixGPU.move_to_backend","page":"API reference","text":"move_to_backend(B, a)\n\nMove the array a to the backend B (a no-op when a is already there).\n\n\n\n\n\n","title":"HMatrixGPU.move_to_backend"},{"category":"function","location":"api/#HMatrixGPU.backend_from_name","page":"API reference","text":"backend_from_name(name) -> KernelAbstractions.Backend\n\nStrict resolver for backend names (\"cpu\", \"cuda\"/\"nvidia\", \"amd\"/\"roc\", \"oneapi\"/\"intel\", \"metal\"/\"apple\"); the backend= keyword uses it. Errors with an actionable message when the vendor package is not loaded or has no functional device. There is deliberately no auto-detection: the user (or a higher-level package) chooses the backend explicitly.\n\n\n\n\n\n","title":"HMatrixGPU.backend_from_name"},{"category":"function","location":"api/#HMatrixGPU.ACA_plus","page":"API reference","text":"ACA_plus(n_rows, n_cols, calc_rows, calc_cols, eps; max_iter=n_rows, max_rank=0,\n         pivot_tol=1e-13, row_block=1, col_block=1)\n\nAdaptive Cross Approximation with the ACA+ look-ahead pivoting strategy (Grasedyck 2005, Construction 2.4; based on Bebendorf 2000 and Bebendorf & Rjasanow 2003) and relative error control, generalized to block rows/columns.\n\nThe pivoting keeps one reference row group and one reference column group of residuals. Each step picks the row-group pivot from the reference column group residual and the column-group pivot from the reference row group residual (look-ahead), completes the cross through the larger of the two candidates, and refreshes the reference when a pivot coincides with it. For row_block = col_block = d > 1 (vector-valued problems such as the demagnetization tensor with three DOFs per cell) the pivot block is d x d and one step captures all d components of a cell together — plain scalar pivoting starves the weak components of anisotropic kernels.\n\nThe stopping criterion is relative: the iteration stops once the Frobenius norm of a rank-block term drops below eps times the Frobenius norm of the block (estimated from a spread sample of row/column groups), so the achieved accuracy is independent of the scale of the block.\n\nFollowing standard practice in H-matrix libraries (Börm–Grasedyck–Hackbusch; AHMED), the iteration is also bounded by a storage crossover rank: once the group rank reaches max_rank (by default ceil(m*n/(m+n)) in groups, the rank at which the low-rank factors cost as much as the dense block), the iteration stops. Blocks whose ε-rank exceeds the crossover are cheaper to store densely, and the caller falls back to a dense block through its regular storage comparison.\n\nArguments\n\nn_rows::Int, n_cols::Int: Number of rows/columns (multiples of the block sizes).\ncalc_rows::Function: Returns the block rows dr x n_cols for a row range.\ncalc_cols::Function: Returns the block columns n_rows x dc for a column range.\neps::Float64: Relative tolerance level for approximation.\nmax_iter::Int: Maximum number of iterations (default is the group count).\nmax_rank::Int: Maximum group rank before aborting; 0 selects the storage crossover ceil(m*n/(m+n)) in groups.\npivot_tol::Float64: Pivot blocks with Frobenius norm below pivot_tol times the estimated block magnitude are treated as zero (protects against degenerate blocks with rows that are zero to machine precision).\nrow_block::Int, col_block::Int: Group sizes (DOFs per point on each side).\n\nReturns\n\nU_ACA::Matrix: n_rows x (k*col_block) left factor (residual column groups).\nV_ACA::Matrix: (k*row_block) x n_cols right factor (residual row groups, normalized by the pivot blocks).\nconverged::Bool: Whether the approximation met the relative stopping criterion, or a sampled relative residual check confirmed the block is captured to within 10*eps. When false, the caller should store the block densely.\n\n\n\n\n\n","title":"HMatrixGPU.ACA_plus"},{"category":"section","location":"getting-started/#Getting-started","page":"Getting started","text":"","title":"Getting started"},{"category":"section","location":"getting-started/#Installation","page":"Getting started","text":"using Pkg\nPkg.add(\"HMatrixGPU\")\n\nThe package itself has zero GPU dependencies — its only non-stdlib dependency is KernelAbstractions.jl. For GPU support, install one of the vendor packages as well: CUDA (NVIDIA), AMDGPU, oneAPI (Intel) or Metal (Apple). You load the vendor package yourself (using CUDA); loading it has no side effects on the library.","title":"Installation"},{"category":"section","location":"getting-started/#The-backend-model","page":"Getting started","text":"There is no automatic detection: the package keeps no backend state, and the landing device is chosen per instance, at construction time, through\n\nbackend = <name or object> — \"cpu\", \"cuda\", \"amd\", \"oneapi\", \"metal\" (or a vendor alias such as \"nvidia\"), or a backend object such as CUDA.CUDABackend();\nCPU() — when no keyword is given.\n\nDevice-resident inputs (K or point sets on a GPU) are legal: they are downloaded once during construction, but they do not decide the placement. An HMatrix stays where it was built: its factors and the matvec are fixed to that device, and a cross-device matvec errors out — the safety net for a mis-requested placement.\n\nbackend= name Hardware Vendor package Backend object\n\"cpu\" CPU — KernelAbstractions.CPU()\n\"cuda\" / \"nvidia\" NVIDIA GPU CUDA.jl CUDA.CUDABackend()\n\"amd\" / \"roc\" / \"amdgpu\" AMD GPU AMDGPU.jl AMDGPU.ROCBackend()\n\"oneapi\" / \"intel\" Intel GPU oneAPI.jl oneAPI.oneAPIBackend()\n\"metal\" / \"apple\" Apple GPU Metal.jl Metal.MetalBackend()\n\nBecause there is no global state, CPU and GPU instances — even from different vendors — coexist in the same process and can be multiplied in alternation.\n\nHMatrixGPU.backend_from_name(name) is the strict resolver that the backend= keyword uses internally — it errors, rather than guessing, when the vendor package is not loaded (backend \"cuda\" requires CUDA.jl — run `using CUDA` first) or has no functional device (backend \"cuda\" was requested but no functional device was detected). There is deliberately no \"gpu\" auto-choice and no silent fallback: a script that wants a soft fallback wraps the call in try/catch itself. The @using_gpu() macro is a plain convenience loader — it loads whichever vendor package the environment provides and does nothing else.","title":"The backend model"},{"category":"section","location":"getting-started/#A-first-example-(CPU)","page":"Getting started","text":"The following complete example compresses the 2D Laplace single-layer potential (a log kernel) between points on a ring, builds the compressed matrix from a kernel function, and validates the product against the exact kernel:\n\nusing HMatrixGPU, LinearAlgebra\n\nN = 800\npts = [(sin(2π*i/N), cos(2π*i/N), 0.0) for i in 1:N]   # one point per element\n\n# 2D Laplace single-layer potential (log kernel)\nH = HMatrix(pts; eta = 1.5, eps = 1e-6, max_points_per_leaf = 64) do x, y\n    d = norm(x - y)\n    d < 1e-12 ? 0.0 : -log(d)/(2π)    # the singular self-pair is g's responsibility\nend\n\ninfo(H)\n\n# validate the compressed product against the exact kernel matrix\nK = [let d = norm(pts[i] .- pts[j]); d < 1e-12 ? 0.0 : -log(d)/(2π) end\n     for i in 1:N, j in 1:N]\nx = rand(N)\nprintln(\"relative error: \", norm(H * x - K * x) / norm(K * x))   # ≈ 1e-8\nrelative error: 2.4303906758192158e-8\n\nWhat this shows:\n\nthe kernel is a function of coordinates: g receives the column views of the i-th target and j-th source point (never indices), and the do x, y ... end form is exactly equivalent to passing a named function;\ng must be a total function — the coincident-pair guard (d < 1e-12) belongs to the kernel, which is why the example can build on a ring where target and source points coincide;\neps is a relative per-block tolerance — the ACA stopping criterion and the SVD recompression truncate at fractions of each block's Frobenius norm — so the achieved accuracy is independent of the scale of the kernel. Blocks whose ε-rank exceeds the storage crossover are stored densely.","title":"A first example (CPU)"},{"category":"section","location":"getting-started/#A-GPU-variant","page":"Getting started","text":"The same example on an NVIDIA GPU — you load the vendor package and name the backend; everything else is identical (fenced, not executed here):\n\nusing HMatrixGPU, CUDA        # installing and loading CUDA is the user's choice\n\nH = HMatrix(pts; eta = 1.5, eps = 1e-6, backend = \"cuda\") do x, y\n    d = norm(x - y)\n    d < 1e-12 ? 0.0 : -log(d)/(2π)\nend\n\ny = H * CuArray(x)   # the matvec runs on the device; x is never moved for you\n\nbackend = CUDA.CUDABackend() — the backend object — is an equivalent spelling. Vectors passed to H * x / mul! must live on the same device as the matrix — the library does not transfer them for you.\n\nAnd because nothing is global, both worlds coexist in one process:\n\nh_cpu = HMatrix(g, pts; eta = 1.5, eps = 1e-6, backend = \"cpu\")\nh_gpu = HMatrix(g, pts; eta = 1.5, eps = 1e-6, backend = \"cuda\")\ny1 = h_cpu * x                # CPU instance\ny2 = h_gpu * CuArray(x)       # GPU instance — interleaved, no interference","title":"A GPU variant"},{"category":"section","location":"getting-started/#Where-to-go-next","page":"Getting started","text":"The manual explains the point-set formats, the kernel contract, the trees, the backend/device rules and every tuning parameter (eta, eps, max_points_per_leaf, block sizes, group size).\nThe examples gallery lists runnable scripts for scalar BEM, covariance and demag-tensor problems.\nCheck known issues for current limitations before running on a GPU.","title":"Where to go next"},{"category":"section","location":"examples/#Examples","page":"Examples","text":"The repository ships five runnable, self-validating example scripts (examples/ — scenario, kernel, mode and measured numbers below are synced from examples/README.md). Every script validates the compressed matvec against the exact kernel (relerr < 1e-5, non-zero exit on failure), takes the problem size as its first command-line argument, and prints a common measurement format (assembly time, block counts, ACA rank range, compression ratio, matvec time, relative error).","title":"Examples"},{"category":"section","location":"examples/#The-gallery","page":"Examples","text":"Script Scenario Kernel Mode Key parameters Default N\nscalar_laplace2d.jl 2D Laplace single-layer BEM on a ring -log(r)/(2π) high-level eta=1.5, eps=1e-6 4000 (4000×4000)\nscalar_laplace3d.jl 3D Laplace single-layer BEM on a Fibonacci sphere 1/(4πr) explicit low-level eta=1.5, eps=1e-6 4000 (4000×4000)\ncovariance_gaussian.jl Gaussian covariance, GP/kriging in the unit cube σ²·exp(-r²/(2ℓ²)), σ=1, ℓ=0.1 high-level eta=1.5, eps=1e-6 4000 (4000×4000)\nvector_demag.jl dipolar demag tensor, thin-film slab, host-loop kernel -(3R_cR_d - r²δ_cd)/(4πr⁵) high-level, dims=3 eta=1.5, eps=1e-4, dims=3 (ACA block sizes follow dims) 2000 (6000×6000)\nhmatrix_vector.jl same tensor, device-side kernel evaluation (advanced) — one work item per cell pair -(3R_cR_d - r²δ_cd)/(4πr⁵) explicit low-level, device block evaluation eta=1.0, eps=1e-6, dims=3, blocks 3×3 2000 (6000×6000)\n\nThe three \"high-level\" scripts are the do-block form of the manual; scalar_laplace3d.jl shows the same workflow in the explicit low-level mode (custom struct + batched getindex), and hmatrix_vector.jl moves the block evaluation itself onto the device with a KernelAbstractions kernel: one work item per cell pair evaluates the geometry once and writes the whole 3×3 block, and the tensor is computed as (3R_cR_d - r²δ_cd)·t with t = -1/(4π·r²·r²·√r²) — one division per pair, and r⁵ decomposed into multiplications plus a single hardware sqrt instead of a software pow.","title":"The gallery"},{"category":"section","location":"examples/#Measured-numbers","page":"Examples","text":"Measured with the default N on a fresh Julia session (times include one-time JIT compilation), an NVIDIA A100, Julia 1.12. Source: examples/README.md. relerr is the relative error of the compressed matvec against the exact kernel; the CUDA columns come from the closing GPU section of each script.\n\nScript assembly compression rank matvec relerr assembly (CUDA) matvec (CUDA) relerr (CUDA)\nscalar_laplace2d.jl 8.1 s 15.8× 4–5 1.28 ms 2.8e-08 7.1 s 0.06 ms 2.8e-08\nscalar_laplace3d.jl 7.5 s 2.4× 9–14 10.82 ms 2.5e-09 6.1 s 0.12 ms 2.5e-09\ncovariance_gaussian.jl 15.7 s 1.3× 10–34 21.57 ms 5.5e-12 12.3 s 0.17 ms 5.5e-12\nvector_demag.jl 12.6 s 2.7× 17–27 22.10 ms 1.7e-10 10.8 s 0.16 ms 1.7e-10\nhmatrix_vector.jl (CPU path) 14.7 s 1.4× 46–100 58.4 ms 1.6e-10 — — —\nhmatrix_vector.jl (CUDA) — — — — — 20.5 s 0.28 ms 1.6e-10\n\nLazy kernels assemble through the CPU ACA in all examples (the queried blocks are evaluated by the kernel, then the factors are placed on the requested backend); only the matvec is device-side — except in hmatrix_vector.jl, where the block evaluation itself already runs on the device. The three high-level scripts pay the wrapper's ≈ 30% assembly-time overhead (see the manual); ranks, compression and accuracy are bit-identical to the equivalent low-level construction. Notes: the 2D log kernel on a ring is the smoothest case (rank 4–5, 15.8× compression); the covariance example at these parameters is dominated by its near-field blocks in 3D, so its compression is modest — the win is the O(N log N) matvec and O(N) storage instead of the O(N²) dense product; the vector demag kernel decays slowly and is anisotropic, so it compresses far less than smooth scalar kernels even with grouped (3×3) pivoting.","title":"Measured numbers"},{"category":"section","location":"examples/#How-to-run","page":"Examples","text":"julia --project=. examples/scalar_laplace2d.jl        # default N\njulia --project=. examples/scalar_laplace3d.jl 2000   # custom N\n\nCPU: every script runs entirely on the CPU out of the box.\n\nGPU: the GPU packages are not installed by HMatrixGPU.jl — run the scripts in an environment that also has the vendor package for your hardware (CUDA, AMDGPU, oneAPI or Metal) with a functional device. Each script's closing GPU section resolves a GPU backend explicitly (HMatrixGPU.@using_gpu() to load whatever the environment provides, then HMatrixGPU.backend_from_name) and is skipped cleanly — with a printed notice — when none is available, so the same script is a CPU smoke test and a GPU benchmark.","title":"How to run"},{"category":"section","location":"examples/#Micromagnetics:-the-demagnetization-field","page":"Examples","text":"A typical application is the demagnetization field in micromagnetic simulations (MicroMagnetic.jl): each time step is one dense matrix–vector product, which is what the 𝓗-matrix accelerates. The tensor assembled in vector_demag.jl and hmatrix_vector.jl is the point-dipole tensor with the self term zeroed — a teaching demo (dims = 3, grouped 3×3 pivoting). Production kernels follow the discretization: finite-difference codes evaluate the Newell analytic cell-pair integrals (regularizing the self term and the near field), and FEM codes use the hybrid FEM–BEM (Fredkin–Koehler) scheme whose compressed operator is the dense boundary matrix B on the mesh boundary (symmetric — the forward product suffices); MicroMagnetic.jl wires this in as its bem_hmatrix demag method. Either way the change is confined to the kernel (or the batched getindex of a low-level K) — the 𝓗-matrix compression interface is unchanged. Build once per mesh, reuse for every time step.","title":"Micromagnetics: the demagnetization field"},{"category":"section","location":"manual/#Manual","page":"Manual","text":"Depth = 2","title":"Manual"},{"category":"section","location":"manual/#Overview","page":"Manual","text":"The package compresses a dense kernel matrix K into an HMatrix and computes y = H * x with workgroup-per-row device kernels. K can be provided in two ways:\n\nHigh-level (recommended) — you write the kernel as a function g(x, y) of two point coordinates and hand the library the points. The library clusters the points, wraps g into a lazy matrix (KernelMatrix), and drives the whole assembly. The bookkeeping that used to be boilerplate (a custom AbstractMatrix struct, size, getindex, cluster trees, block sizes) is gone.\nExplicit low-level — you provide K yourself as any AbstractMatrix and build the trees yourself. This mode is unchanged, is what the high-level mode is a thin layer over, and stays the right tool when you need index-level control, custom storage (e.g. Float32), or device-side block evaluation.\n\nEither way the result is the same HMatrix: a single CSR-resident structure whose matvec runs on every backend.","title":"Overview"},{"category":"section","location":"manual/#Point-sets","page":"Manual","text":"Every point-set entry — ClusterTree, KernelMatrix, and the high-level HMatrix constructors — accepts both forms:\n\na d × N matrix (one point per column). It is used as-is, by reference — no copy, no conversion. This is the native format of MicroMagnetic.jl and of all existing tests and examples.\na vector of points — each element is one point: a d-element tuple, Vector, or SVector. It is converted once, at construction, into the d × N layout (stack, each element becomes one column). The orientation is unambiguous.\n\nInternally everything is d × N; the two forms produce bit-identical trees and matrices.\n\nCoordinates are used exactly as given: the library performs no numerical preprocessing — no scaling, no shifting, no normalization. The geometry reaches the kernel function untouched, and the achieved accuracy does not depend on the scale of the kernel (that is what the relative tolerance eps guarantees).\n\nN × d input (one point per row) is not accepted: in column-major storage a single point's coordinates would not be contiguous, it breaks the cluster tree contract and the ecosystem convention (tdes, HMatrix.jl, MicroMagnetic.jl are all d × N), and for square matrices the orientation is undecidable. If a matrix input looks transposed (row count ≥ 10× the column count, few columns), construction emits a warning once — a hint, nothing is converted:\n\n┌ Warning: point coordinates should be d x N (one point per column); got 2000 x 3 — possible transposed input\n└ @ HMatrixGPU\n\nConstruction convention: write point vectors, or rand(d, N) for random geometries.","title":"Point sets"},{"category":"section","location":"manual/#Kernels","page":"Manual","text":"","title":"Kernels"},{"category":"section","location":"manual/#The-contract","page":"Manual","text":"The high-level entry builds the matrix from a function. The contract is one sentence: K[i, j] = g(x_i, y_j) — g receives the coordinates (column views) of the i-th target point and the j-th source point, not indices.\n\n# named function ...\ng(x, y) = (d = norm(x - y); d < 1e-12 ? 0.0 : -log(d)/(2π))\nH = HMatrix(g, pts; eta = 1.5, eps = 1e-6)\n\n# ... and the do-block form are exactly equivalent\nH = HMatrix(pts; eta = 1.5, eps = 1e-6) do x, y\n    d = norm(x - y)\n    d < 1e-12 ? 0.0 : -log(d)/(2π)\nend\n\nx and y inside the block are d-element column views of the two point sets (no copy; the values are exactly the coordinates you passed in, in whatever form you passed them).","title":"The contract"},{"category":"section","location":"manual/#Vector-kernels:-dims-and-the-DOF-layout","page":"Manual","text":"For a vector-valued kernel (g returns a d × d matrix, dims = d), each point carries d degrees of freedom, and the matrix is the dN × dN operator with the interleaved layout (a single global convention for the whole package):\n\nK[d*(p-1) + c, d*(q-1) + e] = g(x_p, y_q)[c, e]      (dims = d)\nK[p, q]                     = g(x_p, y_q)            (dims = 1)\n\nso the DOFs of each point occupy consecutive positions (x₁,y₁,z₁, x₂,y₂,z₂, ... for d = 3) and matvecs take flat vectors in and out — the same layout the CSR kernels use. You declare dims once, at construction; the trees, the grouped pivoting (below) and the matrix layout all follow.","title":"Vector kernels: dims and the DOF layout"},{"category":"section","location":"manual/#g-must-be-a-total-function","page":"Manual","text":"The library evaluates g on the point pairs the block tree asks for; it never inspects the geometry. Singular pairs (coincident target and source points) are the kernel's responsibility — regularize them inside g:\n\ng(x, y) = (R = x - y; r2 = dot(R, R);        # demag tensor, dims = 3\n           r2 < 1e-24 ? zeros(3, 3) : -(3.0 * R * R' - r2 * I) / (4π * r2^2.5))\n\nThe library does not guess a regularization for you (explicit is better than implicit), but you are not on your own either: a construction-time probe calls g once on an off-diagonal pair (first target × last source, chosen to avoid singular self-pairs) and checks the return value against dims — a scalar for dims = 1, a d × d matrix for dims = d. A mismatch errors immediately at construction instead of blowing up in the middle of an assembly.","title":"g must be a total function"},{"category":"section","location":"manual/#The-two-API-tiers-and-when-to-leave-the-high-level-one","page":"Manual","text":"The high-level constructors do the bookkeeping: trees, the lazy kernel wrapper, block sizes that follow dims, the type probe. The bookkeeping has a small price: measured on the example gallery, the thin wrapper adds ≈ 30% to the assembly time (every block query travels through KernelMatrix's batched getindex, which calls g once per point pair on host views). Ranks, compression and products are bit-identical to the equivalent low-level construction — only the assembly time differs. The low-level constructor\n\nHMatrix(K::AbstractMatrix, X::ClusterTree, Y::ClusterTree; kwargs...)\n\nis unchanged and takes K as you define it — any AbstractMatrix with any internal representation, storage eltype, and evaluation strategy — and is the right tool when that price matters or when you need:\n\nindex-level control — e.g. a kernel whose entries are not a function of two coordinates (mesh-based integral kernels with their own DOF layout);\ncustom storage precision — a kernel with eltype(K) = Float32 stores its factors in Float32 (assembly always computes in Float64);\ndevice-side block evaluation — a batched getindex that launches a KernelAbstractions kernel on the GPU, so ACA block queries never materialize on the host; examples/hmatrix_vector.jl is the complete worked example of this pattern. When writing such a kernel, evaluate one cell pair per work item (with dims = d and the default block sizes the queries arrive as whole cells, so the geometry is computed once and the whole d × d block written), and decompose fractional powers by hand — r⁵ = r²·r²·√r² costs two multiplications and one hardware sqrt, while r2^2.5 compiles to a software pow sequence.\n\nThe high-level mode deliberately does not offer an index-form do-block (do i, j): it would be indistinguishable in the signature from the coordinate form, and passing indices re-opens the point-set orientation trap that the coordinate form closes.","title":"The two API tiers and when to leave the high-level one"},{"category":"section","location":"manual/#Trees","page":"Manual","text":"ClusterTree partitions the points by recursive geometric bisection: a node with more than max_points_per_leaf points is split along the longest axis of its bounding box; leaf sizes therefore bound the near-field block sizes. Typical values: 32–128.\n\nmax_points_per_leaf controls the tree; dims (default 1) declares the DOFs per point: with dims = d the index map is expanded so that each point's d DOFs are consecutive and every node range covers whole points' worth of DOFs. The tree stores the d × N coordinates it was built from (a matrix input is kept by reference; a point-vector input is converted once), so the high-level constructors and the diagnostics can reuse them. Trees are host objects: device point sets are downloaded once at construction — tree construction and high-level kernel evaluation are host-side algorithms.","title":"Trees"},{"category":"section","location":"manual/#Admissibility,-eps,-and-block-sizes","page":"Manual","text":"Two clusters form an admissible pair (eligible for a low-rank approximation) when\n\ndist(X.center, Y.center) > eta * (X.radius + Y.radius)\n\nSmaller eta classifies more blocks as admissible: fewer dense operations, more (and smaller) low-rank blocks. Inadmissible pairs are subdivided until one side is a leaf; those leaf pairs are the near field (dense blocks).\n\neps is a relative, per-block tolerance: the ACA stopping criterion and the SVD recompression truncate at fractions of each block's Frobenius norm, so accuracy is independent of the scale of the kernel. The ACA runs one order tighter than the recompression so the accumulated matvec error stays at the level of eps. Use 1e-6–1e-8 when the product feeds a field or energy computation. Blocks whose ε-rank exceeds the storage crossover m*n/(m+n) — or that fail a sampled residual check — are stored dense: accuracy never depends on a half-converged factorization. Pivoting is deterministic per block (seeded from the block dimensions), so repeated constructions are bit-identical.\n\nACA block sizes follow dims by default. For vector kernels the ACA pivots on d × d groups (set row_block_size/col_block_size to override; the counts must be multiples of them). Grouped pivoting matters for anisotropic kernels: the components of a 3 × 3 demag tensor differ by orders of magnitude (for a thin film N_zz dominates), and scalar per-column pivoting starves the weak components — one group step captures all components of a cell pair together, the factors have k*d nontrivial columns/rows, and the accuracy of every component is controlled jointly. With the default in place a vector user says dims = 3 once and nothing else; the GPU dense fast path (below) ignores both block sizes — its randomized SVD needs no pivoting and is immune to component anisotropy.","title":"Admissibility, eps, and block sizes"},{"category":"section","location":"manual/#Backends-and-devices","page":"Manual","text":"The landing device of an HMatrix is fixed at construction by the explicit backend= keyword (no global default exists):\n\nbackend= (name or Backend object)  >  CPU() when no keyword is given\n\nbackend= accepts a name (\"cuda\", ...) or a backend object (CUDA.CUDABackend()); the name is resolved strictly and errors when the vendor package is not loaded or has no functional device (see getting started).\nDevice-resident inputs are legal but never decide the placement: a device K or point set (CuArray(pts)) is downloaded once during construction (trees and high-level kernel evaluation are host-side algorithms), and the matrix lands on backend=, else CPU().\nTo place an instance next to an existing array, ask for its backend explicitly:\n\nh = HMatrix(K, X, Y; backend = KernelAbstractions.get_backend(x), ...)\n\nWhat ends up on the device is the three CSR operators, the index maps and the product buffers (moved at construction), plus the internal quantities of the GPU dense fast path; the host coordinates (pts, tree coordinates, KernelMatrix targets and sources) always stay on the host.\n\nThe matvec has a hard same-device contract: x, y and the HMatrix must live on the same device. x is never transferred for you — nothing is moved implicitly, at construction or per call. A mismatch is detected with a clear error (this check is the safety net for a mis-requested placement); H * x allocates the result on the matrix's backend. LinearAlgebra.mul!(y, H, x) fully overwrites y.\n\nBecause nothing is global, CPU and GPU instances — different backends, even different vendors — coexist in one process and can be multiplied in alternation without interfering.","title":"Backends and devices"},{"category":"section","location":"manual/#Assembly-paths-and-the-GPU-dense-fast-path","page":"Manual","text":"Which assembly runs depends on K, not on the interface tier:\n\nLazy / matrix-free K (a KernelMatrix, or any other AbstractMatrix that only defines getindex): always the CPU ACA path — blocks are evaluated on demand and factorized by ACA+ with grouped pivoting; the factors are then placed on the landing backend. No eps restriction.\nDense host K::Matrix on a GPU backend with a working device SVD (probed once per backend type, at construction): the batched GPU dense path — the whole matrix is uploaded once and every far-field block is factorized by a single-sided randomized SVD on the device. The path compresses normally for eps ≳ 1e-6; at tighter tolerances its compression degrades and it warns once when more than half of the far blocks fall back to dense storage (see known issues). If the device-SVD probe fails, the path is not taken and assembly falls back to the CPU ACA.\nDense device-resident K (e.g. a CuArray): not on the fast path — it assembles through the CPU ACA, which queries the device matrix block by block (works, but slow at scale).","title":"Assembly paths and the GPU dense fast path"},{"category":"section","location":"manual/#Diagnostics","page":"Manual","text":"info returns statistics: for a ClusterTree the depth and the point-count range of the leaves; for an HMatrix a Dict with matrix size, near-field/far-field block counts, ACA rank range and the compression ratio.\n\nsparsify_hmatrix (not exported; call as HMatrixGPU.sparsify_hmatrix) returns a SparseMatrixCSC containing exactly the near-field blocks of the block tree — everything that would be stored densely. It accepts any AbstractMatrix kernel, so it composes naturally with KernelMatrix.","title":"Diagnostics"},{"category":"section","location":"manual/#Block-structure-and-plotting","page":"Manual","text":"hmatrix_blocks(H) reconstructs the leaf blocks of the assembled HMatrix from its CSR index arrays — a vector of (rows, cols) for the dense blocks and (rows, cols, rank) for the low-rank ones. It works for GPU-resident matrices too: only the index arrays are downloaded. The optional plotting extension renders this structure as a heatmap once Plots.jl is loaded in the session:\n\nusing Plots              # loading Plots activates HMatrixGPUPlotsExt\n\np = plot_hmatrix(H; size = (700, 700))   # teal = dense, amber = low-rank,\n                                         # origin (0, 0) at the bottom left\nsavefig(p, \"hmatrix_pattern.png\")\n\nplot_hmatrix is deliberately bare — no title, no axis labels — so the figure can be captioned externally; nx/ny set the raster resolution and extra kwargs... are forwarded to Plots.heatmap.","title":"Block structure and plotting"},{"category":"section","location":"manual/#Performance-tuning","page":"Manual","text":"Knob Effect\nmax_points_per_leaf Leaf size of the cluster tree. Larger leaves → fewer, denser near-field blocks and faster assembly; smaller leaves → deeper trees, more low-rank blocks, better asymptotics. Typical values: 32–128.\neta Admissibility. Smaller (e.g. 1.0) → more blocks go low-rank (less FLOPs in the matvec, more assembly work); larger (e.g. 2.0) → more dense blocks. Typical values: 1.0–2.0.\neps Relative per-block approximation tolerance (see above): accuracy is independent of the kernel scale. Use 1e-6–1e-8 for field/energy computations.\nsvd_recompress SVD recompression trades assembly time for lower ranks (faster matvec, less memory). Disable with svd_recompress = false when assembly time dominates.\nrow_block_size / col_block_size ACA group sizes; default to the trees' dims (grouped d × d pivoting for vector kernels). Override only for special DOF layouts. Ignored on the GPU dense path.\nHMatrixGPU.set_groupsize(n) Workgroup size of the permutation kernel of the matvec (default 512). A global performance knob that only affects launch configuration — it never changes results; the CSR matvec kernels use a fixed 32-thread workgroup per row. Most users never need to touch it.","title":"Performance tuning"},{"category":"section","location":"#HMatrixGPU.jl","page":"Home","text":"HMatrixGPU.jl compresses dense matrices that arise from kernel interactions (boundary element matrices, dipolar/demagnetization tensors, covariance kernels, log-potentials, ...) into hierarchical matrices (𝓗-matrices) and accelerates the matrix–vector product on GPUs. It is written with KernelAbstractions.jl, so the same code runs on NVIDIA, AMD, Intel and Apple GPUs as well as the CPU.\n\nThe package is vendor-neutral: its only non-stdlib dependency is KernelAbstractions, and its only package extension is an optional plotting one (Plots.jl). You install and load whichever vendor package you want; instances are placed by their backend= keyword alone (nothing specified means CPU), so CPU and GPU matrices — even from different vendors — coexist in one process and can be multiplied in alternation.\n\nTypical applications include boundary-element matrices, covariance and spatial-statistics kernels, and the demagnetization tensors of micromagnetic simulations — for example the fast dense matrix–vector product of the finite-element demagnetization field in MicroMagnetic.jl.","title":"HMatrixGPU.jl"},{"category":"section","location":"#At-a-glance","page":"Home","text":"A kernel matrix from a function g(x, y) and a set of points — the library clusters, compresses and hands you a fast matrix–vector product; the same code runs on the CPU and on GPUs:\n\nusing HMatrixGPU, LinearAlgebra\n\nN = 2000\npts = [(sin(2π*i/N), cos(2π*i/N), 0.0) for i in 1:N]  # point vector: one point per element\n\n# 2D Laplace single-layer potential (log kernel)\nH = HMatrix(pts; eta = 1.5, eps = 1e-6, max_points_per_leaf = 64) do x, y\n    d = norm(x - y)\n    d < 1e-12 ? 0.0 : -log(d)/(2π)      # the singular self-pair is g's responsibility\nend\n\ninfo(H)\ny = H * rand(N)      # compressed matrix–vector product\n\nPages = [\"getting-started.md\", \"manual.md\", \"examples.md\", \"internals.md\", \"api.md\", \"known-issues.md\"]\nDepth = 1","title":"At a glance"},{"category":"section","location":"#Package-contents","page":"Home","text":"Page Content\nGetting started Installation, the backend model, first example\nManual Point sets, kernels, trees, backends & devices, tuning\nExamples Application gallery: scalar BEM, covariance, demag tensor, micromagnetics\nInternals Design of the stable parts: trees, CSR layout, kernels, ACA+, GPU dense assembly\nAPI reference Documented public interface\nKnown issues Current limitations and fixed issues","title":"Package contents"},{"category":"section","location":"internals/#Internals:-design-of-the-stable-parts","page":"Internals","text":"This page documents the parts of the implementation that are considered stable and standard — the pieces a contributor should understand before changing anything, and the invariants they must preserve. The design follows the classical H-matrix construction (Börm–Grasedyck–Hackbusch 2003; Bebendorf 2000; Grasedyck 2005); where a deliberate deviation exists it is called out.","title":"Internals: design of the stable parts"},{"category":"section","location":"internals/#Overview","page":"Internals","text":"coordinates ──► ClusterTree ──► BlockTree ──► build_matrices ──► build_csr_hmatrix ──► HMatrix\n   (points)      geometric       adaptive       ACA+ per block      three CSR ops      matvec\n                 bisection       single-side    look-ahead,          (Int32 indices,     kernels\n                                 split          grouped, error-      cluster-position    (warp/row)\n                                                controlled           columns)\n\nEverything downstream of the cluster tree works on cluster-ordered indices; the two permutations (target_index_map, source_index_map) translate between cluster order and the original ordering at the boundaries of the computation.","title":"Overview"},{"category":"section","location":"internals/#Cluster-tree-(src/tree.jl)","page":"Internals","text":"ClusterTree is a geometric KD-tree over the point coordinates:\n\nEach node holds a contiguous range of the index_map permutation (start_idx inclusive, end_idx exclusive) plus the cluster center (mean of the points) and radius (largest distance from the center to a corner of the node's bounding box).\nA node with more than max_points_per_leaf points is split along the longest axis of its bounding box at the box midpoint; points strictly below the midpoint go to the left child, the rest to the right child.\nThe tree stores the d × N coordinates it was built from. Point-set inputs are normalized to that layout by _point_matrix: matrices pass through by reference, vectors of points are stacked once. No numeric preprocessing happens anywhere — the coordinates are used as given.\n\nInvariants (must hold after any change):\n\nBoth children of an internal node are non-empty. This is guaranteed by the midpoint rule (min < midpoint ≤ max on the split axis) and by turning nodes whose points are all identical (zero extent on every axis) into leaves — such nodes cannot be subdivided.\nThe leaf check (end - start ≤ max_points_per_leaf) runs before any bounding-box computation: empty or small clusters must never reach code that reduces over the points.\nsort(index_map) == collect(1:N): the map is a permutation.\n\nThe dims keyword handles vector-valued DOFs: with dims = d the index map expands so that the d components of point p occupy consecutive positions (d*p-d+1 ... d*p), and every node range is rescaled to cover the expanded indices. The geometry (center/radius) still refers to the point positions. After any dims = d construction, all cluster ranges are multiples of d — this is what makes grouped d × d ACA pivoting safe.\n\nDeliberate deviation from the textbook: the split plane is the box midpoint (standard geometric bisection) rather than the coordinate mean — the mean can be pulled aside by outliers and, for degenerate point sets, used to leave one side empty (the bug this rule replaced).","title":"Cluster tree (src/tree.jl)"},{"category":"section","location":"internals/#Block-tree-(src/block.jl)","page":"Internals","text":"BlockTree pairs a target cluster with a source cluster and decides admissibility:\n\nadmissible  ⟺  dist(c_X, c_Y) > η · (r_X + r_Y)\n\nwith the center/radius taken from the cluster trees. An inadmissible pair is split along the side with the larger radius (single-side split; if one side is already a leaf, the other is split), recursively, until either the admissibility test passes or both sides are leaves — those become near-field (dense) blocks.\n\nTwo standard refinements are implemented:\n\nmerge_dense_matrices! merges every fully-dense subtree into a single larger near-field block (fewer segments per matrix row, which the CSR layout benefits from directly).\nThe single-side adaptive split is an accepted variant of the block cluster tree (the symmetric both-sides split of the classical construction is the other variant); it keeps the near-field blocks smaller for elongated geometries.\n\nThere is deliberately no is_leaf shortcut on admissibility for the root: the whole matrix is never treated as one low-rank block.","title":"Block tree (src/block.jl)"},{"category":"section","location":"internals/#The-three-CSR-operators-(src/hmatrix.jl)","page":"Internals","text":"The compressed matrix stores everything in three flat CSR structures on the landing backend:\n\noperator rows columns content\nnear D matrix rows (cluster order) near-field columns (cluster positions) the dense blocks\nfar V rank rows L = Σ rank far-field columns (cluster positions) the V factors, one row per rank row\nfar U matrix rows (cluster order) rank rows 1:L the U factors\n\nDesign points that must be preserved:\n\nColumn indices are cluster positions (Int32). The input vector is permuted into cluster order once per product (x_buffer), so every kernel access to x is sequential; nothing else needs the original ordering.\nRows of all three operators are in cluster order; the fused kernel scatters the result through target_index_map at the end.\nThe near-field row sets and the U row sets partition the matrix rows (each row appears in exactly one near-field position per overlapping block, and all its blocks are visited); combined with target_index_map being a permutation this means the fused kernel writes every entry of y exactly once — no atomics, no zero-filling, no race conditions.\nIndex arrays are Int32 (halves index traffic of the memory-bound matvec); build_csr_hmatrix guards against overflow.\nAssembly (build_csr_hmatrix) is a two-pass count-then-fill over the block lists, so the CSR row lengths need not be known in advance.","title":"The three CSR operators (src/hmatrix.jl)"},{"category":"section","location":"internals/#Matrix–vector-kernels-(src/mult.jl)","page":"Internals","text":"Three kernels, all workgroup-per-row with a fixed group size of 32 (one warp on CUDA):\n\npermute_to_cluster! — one thread per DOF, coalesced gather.\ncsr_mul_vec_warp! — one warp per rank row: the 32 threads stride through the row, partial sums land in a 32-slot static shared-memory array, and a for s in (16, 8, 4, 2, 1) loop reduces it.\nnear_u_mul_vec_warp! — one warp per matrix row: strides through the near-field and U segments of the row, reduces both, thread 1 scatters through tmap.\n\nRules any new kernel must follow:\n\nNo unguarded global access. Every kernel checks its own index (i <= length(...)) before touching data, so padding threads from a non-divisible ndrange can never fault. (The ndrange of the warp kernels is 32 × rows, always divisible by the group size.)\nNo state that outlives a guarded segment. KernelAbstractions hoists statements into active-lane-guarded segments; a variable assigned inside a guarded segment and read outside it is undefined on padding threads. Loop counters for device-side reductions must live in for loops, not while loops with a hoisted counter.\nNo shared memory, no barriers, no atomics. The workgroup reduction uses static shared memory but no cross-workgroup synchronization; correctness never depends on warp-synchronous execution.\nRow arithmetic is identical on every backend: with the same factors, the CPU and GPU products are bit-identical (verified by the test suite).","title":"Matrix–vector kernels (src/mult.jl)"},{"category":"section","location":"internals/#Cross-approximation-(src/aca.jl)","page":"Internals","text":"ACA_plus is an ACA+ variant with look-ahead pivoting (Grasedyck 2005, Construction 2.4) generalized to grouped pivoting:\n\nOne reference row group r and one reference column group c are drawn from a per-block deterministic RNG (seeded from the block dimensions), so repeated constructions of the same matrix are bit-identical.\nEach step picks the row-group pivot from the reference column residual and the column-group pivot from the reference row residual (look-ahead), completes the cross through the larger candidate, and refreshes the reference when a pivot coincides with it. This is immune to blocks whose first rows are zero to machine precision (the double-layer kernel on coplanar surfaces), which defeat plain partial pivoting.\nWith row_block = col_block = d > 1 (vector-valued kernels) the pivot block is d × d and one step captures all components of a cell pair together; pinv(P) supports rectangular groups. Scalar problems use d = 1, where the update reduces to the classical a ⊗ b/d.\nThe residual is always computed in Float64 regardless of the query precision; the caller converts the factors to eltype(K).\n\nStopping and storage:\n\nRelative stopping: a term is accepted while its norm exceeds eps times the block Frobenius norm estimated from the spread sample.\nStorage crossover cap: the group rank stops at ceil(m·n/(m+n)) — the rank where the factors would cost as much as the dense block. Blocks that hit the cap without converging, or that fail a sampled residual check, are reported with converged = false and stored densely by the caller: accuracy never depends on a half-converged factorization.\nSignificance guards: pivots below pivot_tol · scale (scale = running maximum over sampled and computed residuals) are treated as zero, with dead-group bookkeeping — this is what protects the factorization on blocks containing rows that are zero to machine precision.\n\nSVD_recompress truncates the factors with a relative Frobenius tolerance (eps/10 by the caller's layering), which is the optimal truncated factorization at that tolerance.","title":"Cross approximation (src/aca.jl)"},{"category":"section","location":"internals/#GPU-dense-assembly-(src/assembly_gpu.jl)","page":"Internals","text":"For a dense host K::Matrix on a non-CPU backend whose device SVD works, far-field assembly takes a batched device path (build_matrices_gpu_dense); otherwise it falls back to the per-block CPU ACA. The capability probe device_svd_available(B) runs one tiny SVD on the backend once per backend type and caches the verdict — the probe is also what keeps vendor LAPACK out of the package's own dependency graph.\n\nAlgorithm per far-field block (rows, cols) (the whole K and both index maps are uploaded to the device once):\n\nGather the block from the uploaded matrix with the gather_block! kernel (device ranges of the two cluster maps bound the block; per-block emission, host scalars pass the ranges).\nOne-sided randomized range finder: draw the n × l test matrix Ω from MersenneTwister(bi) (seeded by the block index — repeated assemblies of the same matrix are bitwise identical, the same invariant as the CPU ACA path), project Y = B·Ω, and orthogonalize through the small l × l Gram matrix of the test projection (host eigensolve). This yields an orthonormal range basis with only one device SVD per block (Halko et al.; the randomized range approximation of Dölz et al.).\nSecond Gram pass (CholeskyQR2-style): the first pass loses up to O(eps·κ) orthogonality on fast-decaying spectra; re-orthogonalizing Q through Q'Q restores it.\nRange cutoff tied to eps: eigen-directions below λ_cut = max((eps/50)², 1e-16) are dropped. The (eps/50)² tie keeps the range below the eps/10 truncation threshold at every supported tolerance; the 1e-16 floor keeps the first Gram pass inside its stability bound (κ(G) = 1/λ_cut ≲ 1e16). The l-vector size is crossover + 16 where the crossover is the storage crossover m·n/(m+n).\nTruncation and dual guard: the projected block Q'B gets the single device SVD; the rank r truncates the relative Frobenius tail at eps/10. The block stays dense when either guard fires: S2h[end] > (eps/10)·‖B‖_F (the range could not resolve the truncation — ε-rank beyond what the range finder captured) or (r+1)·(m+n) ≥ m·n (no storage win over the dense block). Both guards feed the same near-field list, so accuracy never rests on a half-resolved range.\nZero-block drop: a numerically zero block (l1 == 0) is dropped entirely; its rows stay covered by the near/U row-set partition, so the fused matvec kernel still writes each of them exactly once (as zero).\nFactors: U = Q·(U₂[:,1:r]·S₂[1:r]') (m × r, materialized) and V = view(V₂, 1:n, 1:r) — the SVD's right factor is already in far-CSR row order (column j = rank row j), so packing broadcasts it without a transpose.\n\nIf more than half of the far blocks hit the dense guards (tight eps), the assembly emits a one-time warning (maxlog = 1); see known issues. row_block_size/col_block_size do not apply on this path — the randomized SVD needs no pivoting and is immune to component anisotropy.\n\nPacking (build_csr_hmatrix_gpu): the near-field values are gathered from the uploaded K by the fill_near_data! kernel; each low-rank block's U factor is written into the CSR u_data array in block-row order — a host-side useg table records, per matrix row, the u_data position the row's factor rows occupy, and fill_u_block! scatters the device factor into exactly those positions. No intermediate dense assembly is ever materialized on the host.","title":"GPU dense assembly (src/assembly_gpu.jl)"},{"category":"section","location":"internals/#Backend-resolution","page":"Internals","text":"The package keeps no backend state; every constructor resolves the landing backend through _resolve_backend(backend): the explicit backend= keyword — a backend object, or a name resolved by backend_from_name — with CPU() when no keyword is given. Device-resident inputs are downloaded once during construction and do not decide the placement; the matvec's same-backend check is the safety net (API_DESIGN v1.1: no like=, no device inference — the placement is always explicit).\n\nbackend_from_name maps names through a small table (\"cuda\"/\"nvidia\" → CUDA, \"amd\"/\"roc\"/\"amdgpu\" → AMDGPU, \"oneapi\"/\"intel\" → oneAPI, \"metal\"/\"apple\" → Metal, \"cpu\" → CPU) and looks up the already-loaded vendor module via Base.loaded_modules — a Vector{PkgId} on Julia ≤ 1.11 and a Dict{PkgId,Module} on ≥ 1.12, handled by _loaded_vendor_module. The backend constructors live in the vendor packages (CUDA.CUDABackend(), ...), not in the KernelAbstractions namespace, so no vendor type is ever referenced by the package itself. The resolver is deliberately strict: an unknown name, a vendor package that is not loaded, or a loaded package without a functional device are all hard errors — there is no soft probing and no auto-detection anywhere in the package; callers that want a fallback try/catch around it themselves.\n\nmove_to_backend(B, a) uploads a host array to B (a no-op when it already lives there); to_backend(like, a) is its data-following convenience form. The matvec kernels read the backends off the arrays they are given — no global is consulted anywhere in the product path.","title":"Backend resolution"},{"category":"section","location":"internals/#What-may-change","page":"Internals","text":"The cluster/block trees, the CSR layout, the kernel scheme and the ACA+ algorithm are considered settled. Expected areas of future work (see the project roadmap): device-resident dense K on the GPU dense fast path, Float32 performance paths, transpose/adjoint products, and the H² / nested-basis variant.","title":"What may change"},{"category":"section","location":"known-issues/#Known-issues-and-limitations","page":"Known issues","text":"","title":"Known issues and limitations"},{"category":"section","location":"known-issues/#GPU-dense-assembly:-supported-eps-range","page":"Known issues","text":"For a dense kernel matrix on a GPU backend (with a working device SVD), far-field assembly takes the GPU fast path (single-sided randomized SVD with per-block packing). The range-finder cutoff is tied to the block tolerance, so the path compresses normally for eps ≳ 1e-6 (at eps = 1e-6 the FEM–BEM cube reaches 2.6× compression with a relative error of 3.5e-10). At tighter tolerances the compression degrades — far blocks fall back to dense storage — and the assembly emits a one-time warning once more than half of the far blocks fall back. The path only applies to dense host K::Matrix on a GPU backend: function kernels and other lazy (matrix-free) K always use the CPU ACA path, which has no eps restriction, and a device-resident dense K (e.g. a CuArray) is not on the fast path (it assembles through the CPU ACA, querying the device matrix block by block).","title":"GPU dense assembly: supported eps range"},{"category":"section","location":"known-issues/#Float64-only-at-the-high-level;-Float32-storage-at-the-low-level","page":"Known issues","text":"The high-level interface — the function-kernel HMatrix constructors and KernelMatrix — is Float64-only: point sets and the values g returns must be Float64.\n\nThe low-level explicit mode supports Float32 storage end to end: assemble from a custom kernel with eltype(K) = Float32 and the factors are stored in Float32 (the assembly always computes in Float64 internally and converts at storage time), with Float32 vectors in and out of the matvec — regression-tested on the CPU and through the GPU dense path (which also computes in Float64 internally and stores in eltype(K)).","title":"Float64 only at the high level; Float32 storage at the low level"},{"category":"section","location":"known-issues/#No-transpose/adjoint-products","page":"Known issues","text":"Only the forward product H * x is implemented. This is sufficient for the target application (the FEM–BEM boundary matrix is symmetric), but H' or transpose(H) products are not available yet.","title":"No transpose/adjoint products"},{"category":"section","location":"known-issues/#Lazy-(matrix-free)-assembly-is-CPU-bound","page":"Known issues","text":"A function kernel (KernelMatrix, the high-level constructors) and any other lazy AbstractMatrix assemble through the CPU ACA: the queried blocks are evaluated on the host and the resulting factors are placed on the target backend — the matvec runs on the device, the assembly queries do not. The only device-side piece available for lazy kernels is the block evaluation itself: a custom low-level kernel whose batched getindex launches a KernelAbstractions kernel (the advanced pattern in examples/hmatrix_vector.jl). A fully device-side assembly exists only for dense host K::Matrix (the GPU dense fast path above).","title":"Lazy (matrix-free) assembly is CPU-bound"}]
}
